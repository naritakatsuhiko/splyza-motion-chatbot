# チャットボット ナレッジ更新・改善ガイドライン（AIエージェント向け指示書）

本指示書は、SPLYZA Motion チャットボットのヘルプナレッジを定期的に更新・最適化するための標準ワークフローです。
今後、ナレッジの更新（NotionサイトのPDF化データの取り込みなど）を行う際は、以下のステップに沿って作業を依頼・実行してください。

---

## 1. ナレッジ更新の標準ステップ

### Step 1: 環境の最新化
1. リポジトリの最新ソースを取得します（`git pull`）。
2. 公開用フォルダ `src/data/knowledge/` および保留用フォルダ `src/data/pending_knowledge/` のファイル一覧と構成を確認します。現時点でチャットボットに含めたくないファイルは必ず保留用フォルダに隔離されていることを確認してください。

### Step 2: 最新PDFの読み込みと「段組み崩れ」の復元（超重要）
1. ユーザーから提供される最新PDFを読み込み、現在のMarkdownナレッジとの差分を確認します。
2. **【最重要注意点】** NotionページをPDF化したデータは、左右の2段組（2カラム）や表（テーブル）レイアウトになっていることが多いため、テキスト抽出時に左右の文章が1行に混ざって認識され、日本語の語順がバラバラに崩れる（寸断・逆転する）現象が発生します。
   - 例: `動画を対象者に紐づけることで、 する。 過去データとの比較を容易に`
3. このような崩れを発見した場合は、必ず意味を汲み取って**「流暢で自然な正しい日本語」**に手動で推敲・復元してMarkdownに記述してください。

### Step 3: ファイル構成と「見出しの並び順」の完全一致化
1. 各Markdownファイル内の見出し（`##` や `###`）の並び順を、最新のヘルプセンター・トップページ（目次PDF）の並び順と**完全に一致**させてください。
2. 目次の位置が変わった場合は、ファイル内のセクション順序や上部の目次リンクも同様に入れ替えて並び替えます。

### Step 4: 「例外事項」の定義ファイルおよび保留フォルダの更新
1. ナレッジディレクトリ内の `EXCLUDED_CONTENTS.md` （除外リスト）を確認・更新します。
2. Googleフォームの設置のみでテキスト解説が存在しないページや、動画埋め込みのみのページなど、**「テキスト解説がないため、意図的にチャットボットナレッジから除外している」**項目をこのファイルに記録します。なお、このファイルはAPI（`route.ts`）側で自動スキップされるよう実装されています。
3. **保留ナレッジの隔離管理**: 現時点で絶対にボットに含めたくない知識（いずれ公開する可能性があるものを含む。投球マニュアルや未公開仕様など）は、絶対に `src/data/knowledge/` に置いてはいけません。必ず `src/data/pending_knowledge/` フォルダの中にファイルを配置、または隔離させてください。
4. これにより、今後の更新担当者やAIが「ファイルが欠落している（更新漏れである）」と誤認するのを防ぎます。

### Step 5: 回答精度向上のためのAPI境界処理の維持
1. `src/app/api/query/route.ts` にて、複数のMarkdownファイルを1つのコンテキスト（テキスト）に結合してLLMに渡す処理が、以下の**XMLタグ形式**に保たれていることを確認します。
   ```typescript
   knowledgeBase += `\n<document title="${file}">\n${content}\n</document>\n`;
   ```
2. この形式により、LLMがファイルごとの境界を正確に識別し、ハルシネーション（情報の混同や誤回答）を防ぐことができます。

### Step 6: 最終確認とデプロイ
1. `git diff` で意図しない変更（不要なスペース、デバッグ用記述など）が含まれていないか最終確認を行います。
2. 変更ファイルを `git add` して、わかりやすいコミットメッセージとともにコミット・プッシュします。
3. Vercelの自動ビルド・デプロイが走り、デプロイ完了を以てリリースとなります。

---

## 2. 【コピペ用】AIエージェントへの一括指示プロンプト

次回、Notionの更新に伴ってPDFデータからチャットボットのナレッジを自動で更新させたい場合は、**以下のプロンプトをそのままCursorのチャット（AIエージェント）に貼り付けて指示を出してください。**
AIが本ガイドラインを理解し、段組み崩れの補正、目次並び順の最適化、XML API構造の維持、例外リストの作成までをすべて自律的に実行します。

```text
SPLYZA Motionのヘルプセンターのナレッジ（Markdown）を最新のPDFデータを基に更新・最適化してください。
更新用の最新PDFは、前回の更新時と同様にローカルディレクトリ、または指定のダウンロードフォルダ内に準備されています。

作業にあたっては、以下の「ナレッジ更新の標準ルール」を厳守してください。

1. 【最優先：日本語の復元】
   NotionをPDF化したデータは、左右の段組みや表レイアウトの影響で「テキスト抽出時に日本語の語順が崩れる・寸断される」バグが高確率で発生します。
   （例：「動画を対象者に紐づけることで、 する。 過去データとの比較を容易に」等）
   このような崩れを発見した場合、そのままマークダウン化せず、必ず人間が読んで流暢で分かりやすい「本来の正しい日本語」に推敲・復元した上で記述してください。

2. 【見出し順の完全一致化】
   各Markdownファイルの見出し（## や ###）の順序は、最新のヘルプセンター・トップページ（目次PDF）の並び順と完全に一致させてください。トップページのレイアウト順が変更されている場合は、Markdown内のセクション順序や目次リンクも同じ順に並び替えてください。

3. 【例外の管理】
   お問合せ用のGoogleフォームのみのページなど、意図的にナレッジから除外しているページは「src/data/knowledge/EXCLUDED_CONTENTS.md」にその理由とともに記録し、ファイル欠落と誤認されるのを防いでください。

4. 【非公開・保留フォルダの遵守】
   現時点で絶対にチャットボットに含めたくない非公開ナレッジ（将来的に公開する可能性のあるマニュアルや社内メモ等）は、絶対に「src/data/knowledge/」に移動させないでください。これらは必ず「src/data/pending_knowledge/」フォルダ内で安全に隔離・保管した状態を維持してください。

5. 【APIのXML結合処理の維持】
   「src/app/api/query/route.ts」において、ナレッジ結合処理が XMLタグ形式（<document title="...">...</document>）になっていることを確認し、これを維持・必要に応じて修正してください。また、`EXCLUDED_CONTENTS.md` の読み込みスキップ処理が維持されていることも確認してください。

作業が完了したら、「何をどのように修正したか（崩れていた日本語のビフォー・アフターを含む）」を非エンジニア向けに分かりやすく整理して報告し、最後にコミットとプッシュまで完了させてください。
```
